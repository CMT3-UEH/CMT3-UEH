# Thư viện cho ỨNG DỤNG Streamlit (bản triển khai trên Streamlit Cloud).
# Chỉ gồm những gói mà app.py và 11 trang trong trang/ thực sự nạp tới.
# Bộ đầy đủ để tải dữ liệu, huấn luyện và chạy notebook nằm ở requirements-dev.txt.
#
# Không có vnstock và yfinance: hai gói này chỉ được nạp bên trong các hàm
# fetch_ohlcv/fetch_macro của src/data/loaders.py, mà ứng dụng không bao giờ gọi
# tới — nó đọc thẳng data/processed/panel_FPT.csv đã có sẵn trong repo. Giữ
# chúng lại thì vnstock kéo theo vnstock-ezchart vốn đòi numpy<2.3, trong khi
# Python 3.14 của Streamlit Cloud cần numpy>=2.3.2 mới có bản dựng sẵn — mâu
# thuẫn không thể giải. Chạy tải dữ liệu trên máy thì dùng requirements-dev.txt.
pandas==2.3.3
numpy==2.3.5
pyarrow==24.0.0
scipy==1.17.1
statsmodels==0.14.6
requests==2.34.2
plotly==6.9.0
streamlit==1.61.1
