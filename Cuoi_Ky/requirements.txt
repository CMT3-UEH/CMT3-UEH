# Thư viện cho ỨNG DỤNG Streamlit (bản triển khai trên Streamlit Cloud).
# Chỉ gồm những gói mà app.py và 11 trang trong trang/ thực sự nạp tới.
# Phần huấn luyện (torch, scikit-learn, lightgbm) và notebook nằm ở
# requirements-dev.txt — cài bộ đó khi chạy run.bat trên máy.
pandas==2.3.3
numpy==2.2.6
pyarrow==24.0.0
scipy==1.17.1
statsmodels==0.14.6
vnstock==4.0.4
yfinance==1.4.1
requests==2.34.2
plotly==6.9.0
streamlit==1.61.1
